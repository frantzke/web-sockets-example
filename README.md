# Basic Sockets Example

Example use of socket.io for web socket communication while running an express server.

## Run it

```
cd server
npm install
npm start
```

Open `app/index.html` in a browser. 